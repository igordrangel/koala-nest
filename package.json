{
  "name": "koala-nest",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "bin": {
    "kl-nest": "./libs/cli/index.ts"
  },
  "scripts": {
    "kl-nest": "bun libs/cli/index.ts",
    "build:koala-nest": "bun scripts/build-koala-nest.mjs",
    "build:cli": "bun scripts/build-cli.mjs",
    "build": "bun build:cli",
    "test": "cd libs/koala-nest && bun test",
    "test:watch": "cd libs/koala-nest && bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/prompts": "^1.5.1",
    "picocolors": "^1.1.1"
  }
}