# Configuração Bun
# Compatível com npm (npm install vai ignorar este arquivo)

[build]
outdir = "dist"

[test]
root = "apps"
testPatterns = [
  "**/*.spec.ts",
  "**/*.e2e-spec.ts"
]
timeoutMs = 60000

[loader]
".ts" = "tsx"