/**
 * Este arquivo é um placeholder que deve ser substituído pelo projeto consumidor.
 *
 * Para usar a lib @koalarx/nest, você precisa:
 *
 * 1. Criar um arquivo com seu Prisma Client gerado no seu projeto:
 *    `prisma/generated/client.ts` ou `prisma/generated/client.js`
 *
 * 2. Criar um arquivo `prisma-client-provider.ts` na raiz do seu projeto com:
 *
 * ```typescript
 * export { PrismaClient, Prisma } from './prisma/generated/client'
 * export type { PrismaClientOptions } from './prisma/generated/internal/prismaNamespace'
 * ```
 *
 * 3. Usar path mapping no seu tsconfig.json:
 *
 * ```json
 * {
 *   "compilerOptions": {
 *     "paths": {
 *       "prisma-client-provider": ["./prisma-client-provider.ts"]
 *     }
 *   }
 * }
 * ```
 */

export {}
