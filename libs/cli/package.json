{
  "name": "@koalarx/nest-cli",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test --max-concurrency=1 test/isolated/ && bun test --max-concurrency=1 test/unit/",
    "test:unit": "bun test --max-concurrency=1 test/isolated/ && bun test --max-concurrency=1 test/unit/",
    "test:e2e": "bun test --max-concurrency=1 test/e2e/"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
