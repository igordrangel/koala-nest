---
'@koalarx/nest-cli': patch
---

Corrigido filtro do cpSync que bloqueava todos os arquivos quando CLI instalado globalmente (o caminho ~/.bun/install continha 'dist' em 'install'), ajustado script de geração do Prisma Client para build:prisma
